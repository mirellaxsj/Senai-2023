#include <stdio.h>
#include <string.h>
#include <locale.h>
int main (){
	
	setlocale(LC_ALL,NULL);
	char direcao[10];
	char comando[20];

	printf("Digite o comando que o soldado deve seguir: ");
	scanf("%s" ,&comando);
}

